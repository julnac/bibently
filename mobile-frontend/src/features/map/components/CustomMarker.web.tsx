import { View } from "react-native";

// Web version - just export a placeholder component
// This won't be used since MapScreen.web.tsx doesn't render markers
export default function CustomMarker() {
  return <View />;
}
